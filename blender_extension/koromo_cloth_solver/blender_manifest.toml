schema_version = "1.0.0"

id = "koromo_cloth_solver"
version = "0.7.1"
name = "Koromo"
tagline = "Bake stitched continuum garments against animated bodies"
maintainer = "ysk424"
type = "add-on"
website = "https://github.com/ysk424/koromo-cloth-solver"

blender_version_min = "5.2.0"
platforms = ["windows-x64"]
license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Load the bundled native solver DLL"

[build]
paths_exclude_pattern = [
  "__pycache__/",
  ".*",
  "*.zip",
]
